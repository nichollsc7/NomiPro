@model IEnumerable<Nomipro.ModelDB.Horario>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Horario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hora_Trabajo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Horario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hora_Trabajo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Horario }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Horario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Horario })
        </td>
    </tr>
}

</table>
